server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JaimeLannisters/{application}
          username: 425359983@qq.com
          password: Sunjikai.123
  application:
    name: config-server-eureka

eureka:
  client:
    serviceUrl:
      defaultZone: http://user:123456@localhost:8761/eureka
  instance:
    prefer-ip-address: true